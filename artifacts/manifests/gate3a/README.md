# Gate 3A manifests

This directory contains small, Git-trackable provenance records for the
repository cleanup. The pre-change manifests identify scientific payloads,
code/provenance files, and the relocated local Kao cache by relative path,
byte count, and SHA-256. Post-change records distinguish content-preserving
path moves from scientific changes.
