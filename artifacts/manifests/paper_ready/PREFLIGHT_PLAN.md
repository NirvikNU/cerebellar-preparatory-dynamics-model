# PAPER-MODELLING-PREPREDICTION-01: ordered preflight

Authority: top Notion contract of 20 September 2026; all three Paper-ready
Modelling child pages read before repository work. Starting HEAD, main,
v3, both tracking refs and direct remote main/v3 equal
37680bad81e3ae441a285b3a4cadf1194df4b5c6. Clean starting index/worktree;
fetch and connectivity check passed. Dangling objects are informational and
will not be pruned. Active path is E:/PROJECTS/Nirvik_Sinha_Data/
cerebellar-preparatory-dynamics-model. No archive dependency.

## Ordered gates

1. Inventory hidden/ignored/untracked files; SHA256-protect all current
   Stage-1/2/3 scientific assets, source, configuration, renderers and runners.
2. Open supplied empirical FIG programmatically. Inventory graphics objects,
   bar values, displayed errors, axes/legend text and embedded UserData.
   Reopen/render without saving over the supplied file. Establish exact Prep
   Control/Block PR and Observed/Expected alignment and error-bar semantics.
   Do not infer missing paired-effect uncertainty from marginal bars.
   Missing required empirical definitions are a dependent-analysis stop.
3. Before numerical calibration, lock full analysis configuration, input hashes,
   equations, feasibility criteria, seeds, statistics and panel provenance.
4. Timing-only deterministic Intact preparation: reuse frozen per-network
   kappa0 and saved Stage-2 P(lambda)/lambda at the eight approved lambdas.
   Intact reduces algebraically to f(x)-f(x*)-(kappa0 I+L)(x-x*), independent
   of the candidate xB. Cue=xsp, native dt=.2 ms, saved1 ms, 500 ms duration.
   Readiness is first post-cue saved sample with E_Q/E_Q(cue)<=.1 and every
   subsequent saved sample through GO<=.1. No interpolation; undefined cue
   or no sustained crossing remains a failure, never replaced by500 ms.
   Median targets then median ten networks; eligible50--100 ms, nearest75.
   If none eligible, stop review without grid expansion.
5. Only after extraction/timing gates, primary native-rate stochastic geometry
   on approved6x6 alpha/beta grid, direction1,30 trials/target, s_init=.10,
   s_temporal=.10, preparation-only. Same standardized draws across all
   policies/candidates, frozen per-neuron normalization/null convention.
   Final100-ms target means; K15 primary with explicit degeneracy exclusion,
   >95% common-K sensitivity. Shared geometry selected by exact prescribed
   normalized squared-effect loss, lexical numerical ties. No kinematic or
   prediction selection. Preserve all infeasible/nonselected evidence.
6. Two separate Intact noise sweeps [0 .05 .10 .20 .40], other source=.10.
   Fixed reference Intact(.10,.10), selected Block(.10,.10); geometry and
   residual variance, no prediction. Movement/event QC for all four policies,
   deterministic unchanged post-GO movement, each trial's own20% peak MO.
7. Main a--d and supporting timing/geometry/noise/QC editable FIG+PNG,
   independent numeric/uncertainty/figure audits, native Notion publication,
   conservative classified cleanup, bounded reference/smoke/preservation.
   Only on successful completion: normal v3 checkpoint/push, safe main FF,
   verify all refs and clean status, update management and stop review.

Panel e and all new prediction fits are prohibited. Accepted Stage-1/2/3
models/results/figures and prior negative outcomes remain unchanged. No
training, architecture changes, archive restoration or outcome-driven tuning.
This preflight plan does not substitute missing empirical metadata or expand
the scientific contract; the full numerical plan is written after source audit.
