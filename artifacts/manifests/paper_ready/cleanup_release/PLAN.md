# PAPER-MODELLING-CLEANUP-RELEASE-01

23 September 2026. Cleanup/release only, starting at
70fff703f8fd3074bef62ca9954a03bef50e4d99 on v3-romano-hennequin.
The initial fetch and direct remote query agree for both v3 and main;
tracked worktree/index are empty and no Git lock was found.

## Boundary

No simulations, scientific analyses, fits, RRR, eta selection, retuning,
scientific source edits or reinterpretation. Preserve accepted Stage-1/2/3,
corrected alignment95, prediction/RRR and stabilization evidence, including
negative results, failed execution provenance, QC flags and required caches.
Only read-only Code Analyzer and existing FIG/PNG packaging checks run.

## Ordered release gates

1. Native Notion snapshots before shortening active pages. The archive is
   https://www.notion.so/3e326c94be3081228b90c49a81c0c600. Preserve all14 original
   gallery images there; the active gallery has ten current images/full legends.
2. Inventory hidden/untracked/ignored files; compare complete protected
   RESUME_INPUTS and OUTPUT_INVENTORY hashes. Classify every new prediction/
   stabilization artifact before any relocation/deletion.
3. Keep code, compact evidence, docs, figure pairs and unique audit receipts.
   Consolidate historical phase/stop notes with byte-preserving relocations.
   Preserve closed nonempty logs as durable text; delete only verified empty
   logs. Never delete a scientific file. Record every original/destination/hash.
4. Read-only Code Analyzer on31 new scientific MATLAB files and the packaging
   verifier. Reopen/decode ten current FIG/PNG pairs without saving/exporting.
   No scientific helper or renderer is called.
5. Review staged paths, content diff, cache exclusion, sizes and preservation;
   make one normal commit with the requested message. No amend/squash/reset.
6. Normal v3 push. Verify main ancestry and expected-old SHA, then guarded
   local fast-forward and normal main push. Stop on divergence/guard failure.
7. Compare HEAD/local/tracking/direct remote v3/main, verify clean tracked
   tree/index, no unexpected untracked files and no locks. Update compact
   Notion status pages and append Agent Log after synchronization succeeds.

The containing commit cannot record its own SHA. The final synchronization
receipt is stored in Git administration (.git/paper-modelling-cleanup-release.json)
and the Notion Agent Log/status pages after push. This avoids a second commit or
amend. Versioned inventories retain all pre-release preservation evidence.

Historical manifests are immutable receipts, not instructions to rerun old
jobs. RELOCATIONS.csv maps their original paths to byte-identical retained
files. Only approved navigation/cross-reference edits may change old docs;
no scientific file is an exception to preservation.
