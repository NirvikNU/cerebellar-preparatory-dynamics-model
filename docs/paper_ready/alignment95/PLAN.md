# PAPER-MODELLING-ALIGNMENT95-01 — locked corrective plan

21 September 2026. Start aebd5fdfd300c19eca85548315357f11ecfd44ac.
Authority: current top Agent Instructions corrective contract. No panel e,
prep-to-movement prediction, regression, training or adaptation.

## Only scientific correction

For each reference Control spectrum, K is the first cumulative proportion
greater than or equal to 0.95. It is not fixed 15, not a maximum with Block K,
not strictly greater, and not forced equal across networks. Use Control K
for Block basis width, Control top-K denominator and null random-subspace
width. PR still uses all eigenvalues. Preserve all old K15/common-K outputs.
Direct covariance/eigendecomposition, threshold-boundary sentinels and QR
projector checks independently verify the complete rule.

Everything else follows the preserved ../PLAN.md: lambda10, frozen kappa0,
V1, alpha=[.1 .2 .35 .5 .75 1], beta_norm=[.1 .25 .5 .75 1 1.25],
all physical/local stability/Euler/rate/state/input feasibility criteria,
30 trials per target, eight targets, ten networks, both preparation noise
amplitudes .10, native .2ms, saved1ms, analysis10ms, native ReLU, no filter,
frozen neuron-preserving normalization and full-reference covariance bias,
GO-100:10:0. Seeds, 10000 null draws and network-bootstrap indices unchanged.
Exact empirical targets and loss/tie rules unchanged. No fitting to movement.
The prior K15 rank screen is retained as a frozen feasibility classification;
every previously tested case had rank77, so it removes no otherwise eligible
case here. New Control K must also be valid for both subspaces.

## Ordered execution and reuse

1. Git health/ref check; inventory hidden/ignored/untracked state; hash all
   previous files, including old paper outputs and raw caches, before writes
   to scientific outputs. Old files are protected, not overwritten.
2. Recompute every tested grid case from saved target-mean trajectories;
   carry forward all physical rejects unchanged. Re-score all common-feasible
   points, select one shared pair with the same relative squared-effect loss.
   Save network/candidate K, cumulative proportion immediately below/at K,
   Block capture, denominator, observed, expected, loss and selection audit.
3. Reference for noise remains standard Intact(.10,.10), not each varied-noise
   condition. Its K governs all noise comparisons. Reuse valid Intact and
   noise trajectories; if geometry changes, refresh geometry-dependent input
   evidence by replay where native states are unavailable. Reuse saved primary
   Intact and selected Block prep; replay only two newly needed partial-removal
   preps and changed movements. Reuse unchanged Intact movement. No timing/grid
   simulation rerun. Same frozen draws in any required replay.
4. For each supporting trailing window use its matching Intact spectrum/K;
   fixed first15/last15 reliability uses the first half as Control. Recompute
   denominator and null scalar on that exact reference covariance. Reuse
   cached projectors for a required K, or generate that K with the frozen seed
   and covariance sampler. Never substitute historical expected scalars.
5. Independently audit selection, K, projection, denominator/null, PR,
   resampling uncertainty, native transitions and all movement event/QC.
   Retain every abnormal trial and negative fit without tuning.
6. New outputs only: results/paper_ready/alignment95; large raw evidence
   results/paper_ready/cache/alignment95; docs/analysis/figures subfolders
   alignment95; four new *_alignment95 FIG/PNG pairs alongside old masters.
   Main a/b and timing remain unchanged; affected d/noise/QC and supporting
   alignment time courses use the corrected analysis. K15 and old common-K
   shown only as explicitly labelled provenance/sensitivity.
7. Code Analyzer, bounded smoke/reference, independent saved-output checks,
   FIG reopen/source/error-bar checks and visual PNG review. Natively publish
   with complete captions; label old K15 evidence historical, preserve it.
   Conservative classified cleanup only. Follow retained normal v3 checkpoint
   and safe main fast-forward conventions after validation; no history rewrite.
   Update paper pages and task/log/handoffs with actual outcome, then STOP.

No grid extension, new noise family, empirical target change, source-unit floor,
model fitting, result-driven threshold change or new inferential family.
