# Execution provenance

23 September 2026. Pre-outcome implementation work only at this entry.

- Bounded Git preflight passed at the requested release, with clean tracked
  worktree/index and no pre-existing untracked content. Normal fetch and direct
  remote reads confirmed both branch SHAs. No branch/ref repair was necessary.
- Read the current Notion contract, released CURRENT_REVIEW and stabilization
  report/captions/helpers. The exact convergence, feature, regression, noise,
  controller preparation and movement implementations are invoked unchanged.
- Locked PLAN.md and a concise page03 prospective entry before new outcomes.
  The eta0 primary fits are retained; only its missing shuffle/matched controls
  are added. Eta1 saved controls and all40 anchor cases are reused.
- The first static checker incorrectly matched its own literal section-marker
  search string. Repaired it to detect only actual section lines. The initial
  failed-start stdout/stderr remain as provenance. No model job ran in that
  failed preflight, and no released code or parameter changed.
- Bounded synthetic leakage/neuron-indexing and independent normal-equation
  ridge checks passed. The synthetic test writes only the new result directory;
  original completed unit-test receipts remain untouched. No RRR test invoked.
- Full protected-file hashing precedes production. Native Windows process-exit
  waits gate the simulation and then analysis/audit/figures; no repeated output
  polling loop is used. Initial discovery saw both shell host and child; the
  verified actual child PID was used for the dependent wait, with no model
  restart. Exceptions stop the dependent sequence.
- The ten existing native Notion PNGs were fetched and SHA256-hashed before
  publication. All names/hashes are recorded in NOTION_IMAGES_BEFORE.json;
  signed access tokens are not retained. The historical archive is not edited.
- All ten native hashes were also compared with their unique local release
  PNGs and matched exactly. Pre-outcome source review confirmed original trial
  stream order, eta-dependent b recomputation, no-reset/no-noise movement,
  trial-specific MO, exact PCA thresholds, mean-SSE ridge/intercept convention,
  global fixed shuffle identities and per-network effect/bootstrap ordering.

Completion and numerical outcomes are recorded only after actual receipts and
publication/preservation checks. This note is not a completion claim.

The initial hash inventory completed at16:57:22UTC:2327 files,
169192893554 bytes. All2293 rows in the release's final preservation manifest
match the freshly computed path/SHA256 records, with zero mismatches. This
includes ignored scientific evidence, not just Git-tracked content. The gated
simulation process launched after the preservation and unit receipts passed.
