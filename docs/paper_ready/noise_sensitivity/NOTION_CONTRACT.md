# Captured prospective Notion contract

Fetched updated 2026-09-23T16:22:11.900Z. Source: https://app.notion.com/p/3c826c94be30817d8f51d9f6c8c2bc19?pvs=204

Here is the result of "fetch" for the Page with URL https://app.notion.com/p/3c826c94be30817d8f51d9f6c8c2bc19 as of 2026-09-23T16:22:11.900Z:
<page url="https://app.notion.com/p/3c826c94be30817d8f51d9f6c8c2bc19" icon="🧭">
<ancestor-path>
<parent-page url="https://app.notion.com/p/3c026c94be3081888fd8f5c59493ccd8" title="Cerebellar Preparatory Dynamics Model"/>
</ancestor-path>
<properties>
{"title":"Agent Instructions — Current Task"}
</properties>
<iconMetadata>{"type":"emoji","emoji":"🧭"}</iconMetadata>
<content>
## Current task — PAPER-MODELLING-NOISE-SENSITIVITY-01
**Authorized 23 September 2026. Final bounded preparation-noise sensitivity diagnostic; not noise calibration or selection of a new primary model.** This task supersedes the prior stop notice only for the work specified here. Start from verified release `93e9d517d47b48579ef9d5bda6adacc44bf039d7`. Preserve the completed calibration, prediction/RRR, stabilization-eta results and the cleaned Notion/archive structure.
### Scientific question and limits
Test how initial cue-state variability and ongoing preparatory process noise separately affect Intact versus full Block convergence and prep-to-early-movement prediction. Do not search for a noise pair that reproduces the empirical R² values, makes convergence positive, or maximizes the condition difference. This is a disclosed follow-up sensitivity analysis motivated by the completed diagnostic, not a blind validation or quantitative model-data fit.
Use two previously evaluated stabilization endpoints: `eta=0` (no added generic residual stabilizer) and `eta=1` (original paper reference). These are diagnostic endpoints, not a newly selected eta. Intrinsic recurrence and target-setting offsets remain present at eta=0. Intact retains the structured prospective feedback L at both eta values; full Block lacks L. No 2×2 equilibrium/feedback counterfactual, partial-removal policy or intermediate eta is authorized.
### Frozen model and fixed noise design
Keep the ten existing networks, eight targets, 30 trials per target, alpha=0.5, beta_norm=1, V realization 1, lambda=10, frozen kappa0 values, L, Q/P, cue states before the noise offset, movement generator, readout, arm and event rules unchanged. Use the already validated eta-dependent controller and b_eta implementation. In particular, `b_eta = f(xB) - f(x*) + eta*kappa0*(x* - xB)`; do not retain b from a different eta.
Run only these two one-factor sweeps, identically for Intact and Block and for both eta endpoints:
- Initial-noise sweep: `s_init = [0.05, 0.10, 0.20]`, with `s_temporal = 0.10` fixed.
- Temporal-noise sweep: `s_temporal = [0.05, 0.10, 0.20]`, with `s_init = 0.10` fixed.
There are five unique `(s_init, s_temporal)` pairs: `(0.05,0.10)`, `(0.10,0.10)`, `(0.20,0.10)`, `(0.10,0.05)`, `(0.10,0.20)`. The common `(0.10,0.10)` anchor must be reused in both sweep displays, not independently simulated or treated as duplicate evidence. Do not run a full factorial noise grid or expand the levels.
Retain the exact existing noise law and streams. The cue offset is `s_init*z`; temporal Euler–Maruyama increments are `s_temporal*sqrt(2*dt/tau)*xi`, with tau=0.15 s and native dt=0.0002 s. Reuse the original standardized initial and temporal draws for each network/target/trial across all levels, conditions and eta values; scale them without reseeding or changing draw indexing. Noise is in source-state units, not Hz or a percentage of signal. Initial noise is a one-time cue perturbation; ongoing process noise also contributes to across-trial variability.
Preparation remains GO−500 to GO; save at 1 ms. Temporal noise and preparation inputs stop at GO, and the achieved state enters the frozen deterministic movement generator without reset. No post-GO noise, observation/spike noise, additional input, altered preparation duration or noise normalization is allowed.
### Reuse and execution boundary
Read the committed `docs/paper_ready/CURRENT_REVIEW.md` and stabilization-eta report/captions/helpers before coding. Inventory/hash protected artifacts and inspect the worktree; do not discard unrelated changes. Reuse the release's verified `(0.10,0.10)` evidence at eta=0 and eta=1, including existing trajectories, C, geometry, events and prediction fits where available. Recover only missing arrays by exact replay and verify identity against saved evidence. Do not repeat entire passed scientific audits or simulations unnecessarily.
Expected coverage is 200 network/condition/eta/noise cases in total: 40 existing anchor cases and at most 160 new cases, each with 240 trials (at most 38,400 new preparation-and-movement trajectories). Do not silently drop networks or targets. Freeze the plan, paths, seeds and interpretation rules before evaluating new outcomes. Exact-equivalence numerical/resource repairs are allowed only with documented checks; scientific changes require a new authorization.
### Primary outcomes — reuse the committed assays unchanged
**Convergence C:** use the exact released stabilization panel-D helper, not the unavailable empirical pre-cue helper. Use unsmoothed ReLU rates, the existing frozen neuron normalization and existing centering convention, sampled at GO−500:10:0 ms. For each network/eta/noise/condition/target, use the same existing three folds: 20 same-target reference trials and 10 held-out trials, with identical memberships across all comparisons. Fit centered PCA from the reference time-by-trial rows only and retain the minimum PCs reaching at least 95% of reference variance; do not whiten or rescale PCs. Project held-out states using that reference-derived transform. Distances are between window-mean projected states and the corresponding reference window means: cue `−500:10:−400 ms`, pre-go `−100:10:0 ms`. Compute each held-out trial's `C = 1 - d_prego/d_cue` and apply the released trial→fold→target averaging to obtain one value per network. Re-estimate reference PCA/means from each dataset under the same fixed rule; do not transfer a different noise level's fitted basis.
Preserve and report `d_cue`, `d_prego`, their ratio, reference PC count/capture, and any undefined denominators as well as C. Use the committed denominator guard; no clamping, arbitrary epsilon rescue, exclusions or changes to aggregation. Negative C is retained. This quantity measures the relative change in held-out deviations from epoch-specific reference means; it is not direct equilibrium stability, not the change in distance to one fixed equilibrium, and not numerically the empirical pre-cue assay. Changing initial noise can change its denominator, so a sign change in C alone is not proof of changed dynamical contraction.
**Prediction R²:** reuse the released PCA→ridge pipeline exactly. Boundary-protected Gaussian SD30-ms smoothing at 1-ms sampling, frozen neuron scales and aligned across-target-invariant subtraction; one window-mean vector per trial from GO−100:10:0 ms and own kinematic MO+0:10:100 ms. PCA is fitted separately per epoch/condition/dataset on the full balanced ensemble, minimum at least 75% variance, as in the committed manuscript-matched assay. Preserve its disclosed non-foldwise feature-learning scope. Use the same target-stratified nested three-fold ridge, 25 penalties `logspace(-8,4,25)`, mean-SSE convention, unpenalized intercept, fixed folds/tie rules and pooled outer-held-out `R2 = 1 - SSE/SST`. All control settings/seeds are reused, not newly selected. Refit only for genuinely new datasets; retain negative R².
Run the existing paired matched-PC control and 100 fixed correspondence-shuffle floor for new datasets using the validated helpers; reuse matching saved anchor controls. No RRR, new decoder, rank analysis, behavioral prediction, movement-end prediction, within-target-only prediction, nonlinear decoder or other regression variant.
### Paired effects and uncertainty
Networks (n=10) remain the independent units. Use the existing fixed 10,000 whole-network bootstrap index rows for median and SE throughout; targets/trials/folds/noise levels are not independent replicates.
For each network and noise/eta setting compute `deltaC = C_Block - C_Intact`, `deltaR2 = R2_Block - R2_Intact`, and `R2_loss_pct = 100*(R2_Intact - R2_Block)/R2_Intact` before summarizing. Positive percent loss means lower Block R²; negative deltaC means lower Block C. Do not calculate paired effects from differences or ratios of marginal medians. When Intact R² is nonpositive or numerically zero, mark the percent loss uninterpretable/undefined and report the raw R² and deltaR²; do not omit or alter those underlying results. Count all undefined values and disclose available paired n. No new inferential test family, significance-based filtering, fitting to empirical percentages or outcome-dependent choice of a noise level.
### Final plots — two compact figure bundles, no heatmap or best-fit panel
**Figure 1: absolute noise-sensitivity curves, four panels.** Columns separate initial-noise and temporal-noise sweeps; top row is PCA→ridge R², bottom row is model-native C. In every panel show Intact and Block separately, using the existing condition colors; solid lines for eta=0 and dashed lines for eta=1. Show all three levels with points and median±network-bootstrap SE, baseline 0.10 marked, and individual network values/lines where legible. Label which noise source varies and which stays fixed. The two source scales share their original units, not an inferred common physiological magnitude. Include C=0 and retain the actual negative range. Do not force R² to \[0,1\] when negative results exist. Put the numerical shuffle floor and matched-PC controls in supporting tables unless needed to explain a main result.
**Figure 2: paired-effect sensitivity, four panels.** Same two columns; top row is paired relative Block R² loss (%), bottom row is deltaC. Distinguish eta=0 and eta=1 consistently. Show zero reference lines, baseline marker, whole-network uncertainty and all prespecified points, including reversals or null differences. The complete source tables also retain absolute deltaR². No fitted optimal-noise region, empirical target bands, regression of C onto R², correlation p-value, selective curve omission or forced monotonic interpolation.
Use native MATLAB FIG plus matching PNG from the same saved arrays, readable legends and `set(gca,'FontSize',16)` unless the existing multi-axis renderer implements an equivalent consistent setting. No MATLAB section breaks. These are proposed Extended Data/robustness figures; do not renumber or replace existing paper figures.
### Mandatory supporting geometry and movement QC
For every setting, tabulate late-preparatory PR, observed/expected alignment and deficit, and Control-derived K under the exact released Control95 rule. These are checks, not a refit of geometry. Also tabulate the released mean GO bias/GO dispersion where available, and the unchanged movement QC: finite-state/input/arm checks, all bounds, own-MO/peak-time/peak-speed summaries, near-zero/missing-window counts, boundary/multiple-large-peak counts, endpoint RMS and target separation/scatter. Do not drop flagged but evaluable trials. If safety/boundedness or required windows fail, retain the failure case and mark affected estimates unevaluable instead of calculating them on a selected good-trial subset. Do not change dynamics, event thresholds, analysis windows or noise levels to rescue a case.
QC is needed to distinguish robustness from effects occurring only when movement becomes pathological; finite or below-null aligned alone is not a sufficient success criterion. This task introduces no new claim of stabilization mediating prediction: changes in noise alter multiple quantities simultaneously, and the sign/magnitude of C depends on its reference and denominator.
### Artifacts, validation, Notion and stopping rule
Put new source/analysis/renderers, compact data, full-precision network/target/trial tables, source captions, locked plan, report and audit receipts under dedicated `paper_ready/noise_sensitivity` subdirectories. Keep required large raw evidence under the existing ignored `results/paper_ready/cache/noise_sensitivity/` convention; never overwrite an old file. Independently verify standardized draw identity, anchor reuse, controller/input equations, new trajectories/features, reference-only PCA/C/distances, regression folds/penalties/held-out R², paired summaries/bootstrap and QC. Use a prespecified bounded sample for independently refitted shuffles and disclose that coverage; check all saved shuffle selections/summaries without claiming a complete independent refit unless done. Run Code Analyzer and existing bounded numerical checks, reopen FIG source objects and visually inspect PNGs. Report mismatches and stop rather than retune.
Publish the two new native figure bundles with full legends in the existing page 02 under a single **Final preparation-noise sensitivity** section. Add concise methods/results/source pointers to page 03 and a balanced result-specific note to page 01. Preserve the ten existing current figures and the historical archive. Keep the cleaned parent/START HERE/Handoff/Instructions concise; append chronology to Agent Log rather than copying historical dumps back into every page. Verify native figure hashes, references and protected-file hashes.
**STOP FOR SCIENTIFIC REVIEW after all fixed points, two figures, supporting tables, audit and Notion updates. No eta/noise selection, model redesign, additional sensitivity or claim of quantitative empirical reproduction. No deletion, staging, commit, push or history rewrite in this scientific run; the prior cleanup-release permission does not authorize a new release.** If the condition difference fails to persist at any tested level, report that limit unchanged. If it persists, limit the conclusion to these one-factor sweeps and the two eta endpoints; interactions or general noise robustness are not tested.
## Last verified release — preserved provenance
**PAPER-MODELLING-CLEANUP-RELEASE-01 completed 23 September 2026.**
Stabilization-eta is complete and numerically validated. Corrected alignment95 and completed prediction/RRR review outputs remain accessible. No eta or noise setting has been adopted as a new primary model. The only new scientific authorization is the bounded noise-sensitivity task above.
Release `93e9d517d47b48579ef9d5bda6adacc44bf039d7` — **Finalize paper modelling prediction and stabilization diagnostics**.
One normal189-file commit, normal v3 push, then safe main fast-forward with the expected-old-SHA guard and normal main push. HEAD, local/tracking/direct-remote main and v3 all match. Active branch remains v3-romano-hennequin. Tracked worktree/index clean; zero untracked files and Git locks. Only required ignored raw/accepted local assets and pinned source/reference caches remain. No force push or history rewrite.
Cleanup preserved all scientific outputs and negative/mixed results. The archive holds seven pre-cleanup page snapshots, including all14 original native images; the current gallery has ten validated figures/full legends.26 historical files were relocated with matching hashes; only three zero-byte logs were deleted. No simulation, fit, RRR, eta selection, parameter change or scientific reinterpretation was performed.
Active repository: `E:\PROJECTS\Nirvik_Sinha_Data\cerebellar-preparatory-dynamics-model`.
Local navigation: `docs/paper_ready/CURRENT_REVIEW.md`.
[Committed cleanup audit](https://github.com/NirvikNU/cerebellar-preparatory-dynamics-model/blob/93e9d517d47b48579ef9d5bda6adacc44bf039d7/artifacts/manifests/paper_ready/cleanup_release/REPORT.md).
Final local synchronization receipt: `.git/paper-modelling-cleanup-release.json`.
Current review: <mention-page url="https://app.notion.com/p/3e026c94be30811dbb4fe27a353fdcd9"/>.
Figures/full legends: <mention-page url="https://app.notion.com/p/3e026c94be30810c8b2ce843260cd7db"/>.
Methods/results/validation: <mention-page url="https://app.notion.com/p/3e026c94be3081b7a1c1dbc406c19e78"/>.
Scientific synthesis/caveats: <mention-page url="https://app.notion.com/p/3e026c94be3081c4bb32d693f6d527bc"/>.
Historical checkpoints/figures: <mention-page url="https://app.notion.com/p/3e326c94be3081228b90c49a81c0c600"/>.
Chronology: <mention-page url="https://app.notion.com/p/3d326c94be3081e897a2e5e0c855c4c0"/>.
</content>
</page>
